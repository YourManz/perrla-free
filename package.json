{
  "name": "perrla-free",
  "version": "0.1.0",
  "private": true,
  "description": "Free, open-source academic paper formatter with APA, MLA, Chicago, and IEEE citation support",
  "license": "MIT",
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter core build && pnpm --filter web build",
    "preview": "pnpm --filter web preview",
    "typecheck": "pnpm --filter core typecheck && pnpm --filter web typecheck",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean && rm -rf node_modules"
  },
  "workspaces": [
    "packages/*"
  ]
}
